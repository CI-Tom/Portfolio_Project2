const playerTurn = document.getElementById('player-turn')
const newGame = document.getElementById('restart')
const tiles = Array.from(document.getElementsByClassName('game-tile'))

console.log(tiles)
